---
const { href, class: className } = Astro.props
---

<a href={href} role="link" class={`inline-flex items-center text-lg font-medium text-teal-600 hover:text-teal-700 ${className ?? ""}`}>
  <slot />
</a>
